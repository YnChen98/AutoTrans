% Stage 4-AV RA-L-oriented scaffold.
% Template source: paper/templates/ral/root.tex.
% Detected documentclass: \documentclass[letterpaper, 10 pt, conference]{ieeeconf}.
% TODO: Confirm final RA-L / IEEE template requirements and page budget before submission.
\documentclass[letterpaper, 10 pt, conference]{ieeeconf}

\IEEEoverridecommandlockouts
\overrideIEEEmargins

\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{cite}
\usepackage{amsmath}
\usepackage{array}
\usepackage{tikz}
\usetikzlibrary{arrows.meta,positioning,fit,calc}

% Display paper-facing interface names even in legacy scaffold prose that still
% uses implementation tokens.
\makeatletter
\let\StageOriginalTexttt\texttt
\renewcommand{\texttt}[1]{%
  \def\StageTextttArg{#1}%
  \def\StageSpeedToken{speed\_scale}%
  \def\StageAccelerationToken{acceleration\_scale}%
  \ifx\StageTextttArg\StageSpeedToken
    speed scale%
  \else\ifx\StageTextttArg\StageAccelerationToken
    acceleration scale%
  \else
    \StageOriginalTexttt{#1}%
  \fi\fi
}
\makeatother

\title{\LARGE \bf
Risk-Conditioned Execution Governance for Balanced Robustness in Windy Suspended-Payload UAV Transport}

\author{TODO: Author names and affiliations}

\begin{document}

\maketitle

\begin{abstract}
Suspended-payload UAV transport in strong wind is sensitive to runtime
execution aggressiveness: even with payload-aware planning, MPC, and low-level
control, the same planned motion can become too aggressive or overly
conservative under different wind and mission conditions. We propose a drop-in
risk-conditioned execution governor that adapts command execution through the
existing speed scale and acceleration scale interface,
leaving the planner/MPC/controller stack unchanged. To avoid a misleading
single aggregate, we evaluate strong-wind behavior under two protocols: a
single-goal mission protocol (Goal Repeat = 1) and a goal-reissue
stress protocol (Goal Repeat = 10). The results reveal strong protocol
specialists: Wind-Level 0.85 has the highest observed single-goal count with
26/30 strict-valid runs, whereas Fixed Scale 0.80 has the highest observed
stress count with 24/30 strict-valid runs. Among the evaluated learned /
risk-conditioned methods, Risk Adapter v1 is the most balanced, achieving the
highest mean valid count (24.0/30), highest worst-protocol count (23/30), and
lowest total regret (2) relative to the protocol oracles.
Invalid-only failure groups and representative traces further show that
aggregate success hides distinct failure modes, motivating failure-aware
interpretation. These results support balanced robustness analysis under the
tested protocols while making no statistical significance claim, no formal
safety guarantee, and no broad hardware-transfer claim.
\end{abstract}

\input{sections/01_intro}
\input{sections/02_related_work}
\input{sections/03_method}
\input{sections/04_experiments}
\input{sections/05_results}
\input{sections/06_discussion}
\input{sections/07_conclusion}

\bibliographystyle{IEEEtran}
\bibliography{refs}

\end{document}
