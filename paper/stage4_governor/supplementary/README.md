# Supplementary Material Placeholder

Add supplementary material here after the main manuscript scaffold stabilizes.

Planned items:

- full run tables
- full protocol split summaries
- all representative trace plots
- failure mode tables
- launch settings
- strict-valid definition
- `risk_adapter_v1` parameters
- `risk_adapter_v2` single-goal-only note
- duplicate CSV audit notes

Claim boundaries remain the same as the main paper: no statistical
significance claim, no safety guarantee, no learned uniform domination, no
mixed-protocol aggregate as the main result, and no `risk_adapter_v22`.
